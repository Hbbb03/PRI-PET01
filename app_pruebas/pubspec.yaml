name: app_pruebas
description: A new Flutter project.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.1.0 <4.0.0'

dependencies: 
  flutter:
    sdk: flutter  
  qrscan: ^0.3.3
  mongo_dart: ^0.9.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  image_picker: ^1.0.4

flutter:
  uses-material-design: true
